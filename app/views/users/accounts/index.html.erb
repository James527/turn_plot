<div class="modal-wrapper">
	<div class="modal">
		<h2>All Accounts</h2>
		<ul class="accounts">
			<% @users.each do |user| %>
				<li><a href='/user/<%= user.id %>'><%= user.username %></a> |
					<%= link_to 'Edit', edit_user_registration_path %></li>
			<% end %>
		</ul>
		<br>
	</div>
</div>