<div class="modal-wrapper" id="editLetterWrapper">
	<div class="modal" id="editLetter">
		<h1>Editing Letter</h1>
		<%= render 'form' %>
		<%= link_to 'Back to Letter', correspondent_letter_path %> |
		<%= link_to 'Back to Correspondent', correspondent_path(@letter.correspondent.id) %>
	</div>
</div>